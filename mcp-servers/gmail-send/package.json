{
  "name": "gmail-send-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for sending emails via Gmail",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node index.js",
    "test": "node -e \"const m=require('./index.js'); if (typeof m.sendEmail !== 'function') process.exit(1); console.log('gmail-send smoke test passed')\""
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "googleapis": "^171.4.0"
  }
}
